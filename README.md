# لوحة التحكم - ائتلاف أساس العراق

## 📋 نظرة عامة

تطبيق إداري متكامل لإدارة بيانات الناخبين في ائتلاف أساس العراق. يدعم التطبيق العمل على الهواتف والأجهزة اللوحية وأجهزة الكمبيوتر المكتبية.

**صوتك اليوم مستقبلك غداً**

## ✨ المميزات الرئيسية

### 🔐 نظام المصادقة
- تسجيل دخول للمرشحين (رمز خاص لكل مرشح)
- تسجيل دخول للمدير (Admin) مع صلاحيات كاملة
- المرشحون يرون فقط مستخدميهم
- المدير يرى جميع البيانات

### 📊 لوحة التحكم
- إحصائيات شاملة ومباشرة
- عدد المستخدمين الكلي
- عدد الأفراد
- المجموع الكلي (مستخدمين + أفراد)
- تصنيف حسب الجنس (ذكور/إناث)
- عدد المنتخبين
- عدد الأقضية
- عدد المرشحين

### 🎯 الفلاتر المتقدمة
- فلتر حسب المرشح
- فلتر حسب القضاء
- فلتر حسب الجنس
- فلتر حسب حالة الانتخاب (تم/لم يتم)
- إمكانية دمج الفلاتر
- إعادة تعيين الفلاتر

### 🔍 البحث الشامل
- البحث في جميع الحقول
- البحث بالاسم
- البحث برقم الهاتف
- البحث بالمدرسة
- البحث بالقضاء
- البحث بالمرشح
- البحث باسم الأب

### 👥 إدارة المستخدمين
- عرض بطاقات المستخدمين بشكل جميل
- عرض الصورة الأمامية
- حالة الانتخاب (تم/لم يتم)
- معلومات كاملة لكل مستخدم
- تعديل بيانات المستخدم
- حذف المستخدم
- تحديد متعدد للمستخدمين

### 📱 التواصل عبر واتساب
- إرسال رسالة لمستخدم واحد
- إرسال رسائل جماعية للمحددين
- رسائل مخصصة باسم المستخدم

### 🔔 نظام الإشعارات
- إشعار للمستخدمين الجدد
- إشعار عند الانتخاب (رفع صورة)
- جرس الإشعارات في الأعلى
- النقر على الإشعار للانتقال للمستخدم

### 📍 الأقضية
- قائمة بجميع الأقضية
- عدد المستخدمين لكل قضاء
- الترتيب حسب العدد
- فلترة سريعة بالنقر

### ⚙️ الإعدادات (قيد التطوير)
- نسخ احتياطي من/إلى الجهاز
- نسخ احتياطي من/إلى قاعدة البيانات
- رفع البيانات
- إعادة ضبط المصنع
- إدارة حسابات المدراء

### 📱 متوافق مع جميع الأجهزة
- تصميم متجاوب (Responsive)
- يعمل على الهواتف
- يعمل على الأجهزة اللوحية
- يعمل على أجهزة الكمبيوتر
- تطبيق Electron لسطح المكتب

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية
- Node.js (النسخة 16 أو أحدث)
- npm أو yarn
- حساب Firebase

### خطوات التثبيت

#### 1. إعداد Firebase

1. قم بزيارة [Firebase Console](https://console.firebase.google.com/)
2. أنشئ مشروع جديد أو استخدم المشروع الحالي
3. انسخ إعدادات Firebase (API Key, Database URL, إلخ)
4. افتح ملف `app.js` وضع إعدادات Firebase:

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "messageemeapp.firebaseapp.com",
    databaseURL: "https://messageemeapp-default-rtdb.firebaseio.com",
    projectId: "messageemeapp",
    storageBucket: "messageemeapp.appspot.com",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

5. في Firebase Console، انتقل إلى Realtime Database
6. استورد البيانات من ملف JSON المرفق

#### 2. تشغيل كتطبيق ويب (PWA)

```bash
# افتح الملف مباشرة في المتصفح
# أو استخدم خادم محلي
npx serve .
```

ثم افتح المتصفح على العنوان المعروض (عادة http://localhost:3000)

#### 3. تشغيل كتطبيق Electron (سطح المكتب)

```bash
# تثبيت المكتبات
npm install

# تشغيل التطبيق
npm start
```

#### 4. بناء تطبيق Electron للتوزيع

```bash
# لنظام Windows
npm run build:win

# لنظام macOS
npm run build:mac

# لنظام Linux
npm run build:linux

# لجميع الأنظمة
npm run build
```

## 🔑 بيانات تسجيل الدخول الافتراضية

### حساب المدير (Admin)
- **المستخدم**: مدير النظام (Admin)
- **الرمز السري**: `admin123`
- **الصلاحيات**: عرض جميع البيانات والمستخدمين

### حسابات المرشحين
يتم إنشاء حسابات المرشحين تلقائياً من البيانات الموجودة في قاعدة البيانات. كل مرشح يحتاج إلى رمز سري خاص به.

**مهم**: يجب تغيير كلمات المرور الافتراضية قبل الاستخدام الفعلي!

## 📖 دليل الاستخدام

### 1. تسجيل الدخول
- افتح التطبيق
- اختر المرشح أو "مدير النظام"
- أدخل الرمز السري
- اضغط "تسجيل الدخول"

### 2. الصفحة الرئيسية
- عرض الإحصائيات الشاملة
- استخدام البحث الشامل
- تطبيق الفلاتر
- عرض بطاقات المستخدمين

### 3. البحث
- اكتب في خانة البحث أي معلومة
- يتم البحث في جميع الحقول
- النتائج تظهر فوراً

### 4. الفلاتر
- اختر الفلاتر المطلوبة
- اضغط "تطبيق الفلاتر"
- لإلغاء الفلاتر اضغط "إعادة تعيين"

### 5. بطاقات المستخدمين
- انقر على البطاقة لعرض التفاصيل الكاملة
- استخدم الأزرار للتواصل أو التعديل أو الحذف
- حدد عدة بطاقات للمراسلة الجماعية

### 6. الإشعارات
- انقر على الجرس لعرض الإشعارات
- انقر على إشعار للانتقال للمستخدم المعني
- امسح الإشعارات عند الحاجة

### 7. القائمة الجانبية
- انقر على ☰ لفتح القائمة
- تصفح الأقضية وأعدادها
- انقر على قضاء للفلترة حسبه

## 🗂️ هيكل الملفات

```
.
├── admin-dashboard.html    # الصفحة الرئيسية
├── app.js                  # منطق التطبيق وFirebase
├── manifest.json           # إعدادات PWA
├── service-worker.js       # Service Worker للعمل بدون إنترنت
├── electron-main.js        # تطبيق Electron الرئيسي
├── package.json            # إعدادات npm وElectron
└── README.md              # هذا الملف
```

## 🔧 التخصيص

### تغيير الألوان
افتح ملف `admin-dashboard.html` وعدّل المتغيرات في `:root`:

```css
:root {
    --primary-color: #1a7b7f;
    --secondary-color: #0d5456;
    --accent-color: #25d366;
    /* ... المزيد من الألوان */
}
```

### إضافة مرشح جديد
سيتم إضافة المرشحين تلقائياً من البيانات. لإضافة كلمة مرور لمرشح:

في ملف `app.js`:
```javascript
const candidatesPasswords = {
    "admin": ADMIN_PASSWORD,
    "اسم المرشح": "كلمة_المرور"
};
```

### تخصيص الرسائل
في ملف `app.js`، وظيفة `sendWhatsapp`:
```javascript
const message = `مرحباً ${name}، رسالتك المخصصة هنا`;
```

## 🔒 الأمان

- **تغيير كلمات المرور الافتراضية** قبل الاستخدام
- استخدام **HTTPS** في الإنتاج
- تفعيل **قواعد الأمان في Firebase**
- عدم مشاركة بيانات الدخول
- عمل نسخ احتياطية دورية

### قواعد أمان Firebase الموصى بها

```json
{
  "rules": {
    "users": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```

## 📱 التطبيق كـ PWA

التطبيق يدعم PWA ويمكن تثبيته على الهاتف:

1. افتح التطبيق في Chrome/Safari
2. اختر "إضافة إلى الشاشة الرئيسية"
3. سيظهر أيقونة التطبيق على الشاشة الرئيسية
4. يعمل بدون إنترنت (بعد أول زيارة)

## 🐛 حل المشاكل الشائعة

### المشكلة: التطبيق لا يظهر البيانات
**الحل**: تأكد من:
- صحة إعدادات Firebase
- الاتصال بالإنترنت
- وجود بيانات في قاعدة البيانات

### المشكلة: لا يمكن تسجيل الدخول
**الحل**: تأكد من:
- كتابة كلمة المرور بشكل صحيح
- اختيار المرشح الصحيح
- إعداد كلمات المرور في الكود

### المشكلة: الصور لا تظهر
**الحل**: تأكد من:
- رفع الصور في Firebase Storage
- صحة روابط الصور
- صلاحيات القراءة في Storage

### المشكلة: الإشعارات لا تعمل
**الحل**: 
- امنح إذن الإشعارات للمتصفح/التطبيق
- تحقق من Service Worker

## 📞 الدعم والمساعدة

للحصول على المساعدة أو الإبلاغ عن مشكلة:
- تواصل مع فريق التطوير
- راجع التوثيق في التعليمات البرمجية

## 📄 الترخيص

هذا المشروع مرخص بموجب MIT License

---

## 🎯 الخطط المستقبلية

- [ ] إضافة التقارير والتحاليل البيانية
- [ ] نظام النسخ الاحتياطي الكامل
- [ ] إدارة حسابات المدراء
- [ ] تصدير البيانات إلى Excel
- [ ] إحصائيات متقدمة ورسوم بيانية
- [ ] نظام الرسائل الجماعية المتقدم
- [ ] تطبيق الهاتف المحمول (Android/iOS)

---

**صوتك اليوم مستقبلك غداً** 🇮🇶

ائتلاف أساس العراق © 2025
